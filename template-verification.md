# Expanded Template Verification

The template editor now shows eight selectable layouts: Poll panel, Breaking line, Big question, Feature story, Signal alert, Spotlight, Photo frame, and Quick bulletin. The new gallery renders as a two-row desktop grid with all labels and intended layout descriptions visible.

The live preview retains the current selected-template composition, headline, metadata, and reaction row while new templates are available for selection.

The Spotlight card was selected programmatically from the rendered template gallery. Its selected state updated correctly and the live artwork changed to the `template-spotlight` treatment, confirming preview selection wiring for the new layouts.

Visual inspection confirmed the Spotlight artwork presents a centered dark key-point card with a saffron border and label, while retaining the selected headline, badge, and page-name metadata.
